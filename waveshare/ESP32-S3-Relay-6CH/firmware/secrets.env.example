WIFI_SSID=your-ssid
WIFI_PASS=your-password
# base64 of 32 random bytes: python3 -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
NOISE_PSK=
DEVICE_NAME=relay6
FRIENDLY_NAME=Relay board
